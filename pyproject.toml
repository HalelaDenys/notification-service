[project]
name = "notification-service"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi[all]>=0.136.1",
    "faststream[redis]>=0.6.7",
    "redis>=7.4.0",
    "uvicorn>=0.47.0",
]
